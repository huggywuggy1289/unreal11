#include <iostream>
#include <string>

using namespace std;

/*
int main() 
{
	int age = 23;
	int hp(100); // int hp = 100;
	int level(1); // "

	float height = 180.5f;
	double PI = 3.141592;
	char grade = 'A';
	bool isAlive = true; // stack에 isAlive라는 이름으로 bool형 변수가 생기고 true(내부적으로는 1)이 저장됨.

	cout << "나이: " << age << endl;
	cout << "체력: " << hp << endl;
	cout << "레벨: " << level << endl;
	cout << "신장: " << height << endl;
	cout << "원주율: " << PI << endl;
	cout << "학점: " << grade << endl;
	cout << "살아있음: " << isAlive << endl;
	isAlive = false; // 값이 덮어씌워짐. 기존 공간 재사용 (덮어쓰기)
	cout << "isAlive? : " << (isAlive ? "Yeah~" : "No...") << endl;
}
*/