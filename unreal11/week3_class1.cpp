#include <iostream>
using namespace std;

/*
void function(string name) {
	cout << "my name is " << name << endl;
}

string GetName(int num) {

	string name = "";
	switch (num)
	{
	case 1 :
		name = "jae yun";
		break;

	case 2:
		name = "wolf keum";
		break;

	default: // 그외 어떤 모든값
		name = "world";
		break;
	}
	return name;
}
int main() {
	int num;
	
	// C++11 이전: 파싱 실패 시 값이 어떻게 될지 표준에 명확히 안 정해져 있어서 컴파일러마다 다르게 동작
	// C++11 이후 : 파싱 실패 시 항상 0으로 설정됨
	
	cin >> num; // 기댓값이 아닌경우 cin 파싱 실패 → num은 강제로 0이 됨
	string name = GetName(num);
	function(name);
	return 0;
}
*/