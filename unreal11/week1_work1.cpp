#include <iostream>
#include <string>

using namespace std;
/*
int main() {
	int a = 0;
	int b = 0; // b가 0일 경우?

	cout << "두 개의 정수를 입력하세요: ";
	cin >> a >> b;

	int c = a + b; // 합
	int d = a - b; // 차
	int e = a * b; // 곱

	cout << "두 숫자의 합은 " << c << "입니다." << endl;
	cout << "두 숫자의 차는 " << d << "입니다." << endl;
	cout << "두 숫자의 곱은 " << e << "입니다." << endl;

	if (b != 0) {
		float f = (float)a / b; // a와 b가 둘다 int라 소수점까지 나타내려면 형변환을 해줘야한다.
		cout << "두 숫자의 몫은 " << f << "입니다." << endl;
	}
	else {
		cout << "b는 0일 수 없습니다." << endl;
	}

	return 0;
}
*/