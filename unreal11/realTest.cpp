#include <iostream>
#include <string>

// 1 int, char, float, bool

// 2
/*
int main() {
	int score;
	const int MaxScore = 100;

	score + 10; // (1) score값이 초기화 되지않아서 쓰레기값 가지고 있음. 또한 이꼴표시가 없어서 값이 어디서도 저장이 안됨.
	MaxScore = 200; // (2) const로 선언된 변수는 값 변경 불가
	char myGrade = "A"; //(3) char형 변수는 작은 따옴표('')로 감싸야함

	return 0;
}
*/